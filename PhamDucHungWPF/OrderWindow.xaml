<Window x:Class="PhamDucHungWPF.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhamDucHungWPF"
        mc:Ignorable="d"
        Title="OrderWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Product " FontWeight="Bold" Grid.Row="0" />

        <ListBox x:Name="listBoxProducts" Grid.Row="1" SelectionMode="Multiple" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="btnPlaceOrder" Content="Order" Width="100" Click="btnPlaceOrder_Click"/>
            <Button x:Name="btnViewOrders" Content="View Orders" Width="120" Margin="10,0,0,0" Click="btnViewOrders_Click"/>
        </StackPanel>

        <ListBox x:Name="listBoxOrders" Grid.Row="3" Margin="0,10,0,0" SelectionChanged="listBoxOrders_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="6" Padding="6" Margin="4" Background="WhiteSmoke">
                        <StackPanel>
                            <TextBlock Text="{Binding OrderId, StringFormat='Đơn hàng #{0}'}" FontWeight="Bold" FontSize="14" />
                            <TextBlock Text="{Binding OrderDate, StringFormat='Ngày đặt: {0:dd/MM/yyyy}'}" />
                            <TextBlock Text="{Binding OrderAmount, StringFormat='Tổng tiền: {0:N0}đ'}" Foreground="DarkGreen" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
